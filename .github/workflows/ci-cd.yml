name: CI/CD Pipeline (Node.js 22 + npm + Mocha)

on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest

    steps:
      # Étape 1 : Récupérer le code
      - name: Checkout repository
        uses: actions/checkout@v4

      # Étape 2 : Configurer Node.js 22
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      # Étape 3 : Installer les dépendances
      - name: Install dependencies
        run: npm ci

      # Étape 4 : Exécuter les tests Mocha
      - name: Run tests
        run: |
          echo "🚀 Lancement des tests avec Mocha..."
          npm test

      # Étape 5 : Connexion à Docker Hub
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Étape 6 : Construction de l'image Docker
      - name: Build Docker image
        run: |
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/node-app:latest .

      # Étape 7 : Tests dans le conteneur Docker
      - name: Run tests inside Docker container
        run: |
          echo "🧪 Tests dans le conteneur Docker..."
          docker run --rm ${{ secrets.DOCKERHUB_USERNAME }}/node-app:latest npm test

      # Étape 8 : Push de l'image vers Docker Hub (uniquement sur master)
      - name: Push Docker image
        if: github.ref == 'refs/heads/master'
        run: |
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/node-app:latest
